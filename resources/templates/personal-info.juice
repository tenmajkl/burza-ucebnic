{ extends 'layout' }

{ block 'title' } 
    { text app.personal-info-title }
{ /block }

{ block 'content' }
    <main class="w-screen h-screen flex flex-col items-center justify-center p-4 gap-1">
        <div class="text-left flex flex-col sm:w-1/2 2xl:w-1/6">
            <h1 class="font-bold text-xl">{ text app.personal-info-title}</h1>
            { text app.personal-info }
        </div>
    </main>
{ /block }
